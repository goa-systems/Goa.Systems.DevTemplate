name: develop
on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "develop" ]
  workflow_dispatch:
  
jobs:
  build:
    runs-on: docker-self-hosted
    steps:
      - uses: actions/checkout@v4
      - name: Run a multi-line script
        run: |
          echo "Building version $(cat version) to be deployed to $HOST_DEVELOP"
